import {createStore} from 'vuex'

// Create a new store instance.
const store = createStore({
  state: {
    current_country: {},
    countries: [],
  },
  getters: {},
  mutations: {},
  actions: {}
})

export default store;