import {createStore} from 'vuex'

// Create a new store instance.
const store = createStore({
  state: {
    currentMovie: {},
    movies: [],
  },
  getters: {},
  mutations: {},
  actions: {}
})

export default store;